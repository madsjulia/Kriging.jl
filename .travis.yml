language: julia
os:
  - osx
  - linux
julia:
  - 1.0
notifications:
  email: false
# script:
#   - if [[ -a .git/shallow ]]; then git fetch --unshallow; fi
#   - julia -e 'Pkg.clone(pwd()); Pkg.build("Kriging"); Pkg.test("Kriging"; coverage=true)';
after_success:
  - julia -e 'import Pkg; cd(Pkg.dir("Kriging")); Pkg.add("Coverage"); using Coverage; Coveralls.submit(Coveralls.process_folder()); Codecov.submit(Codecov.process_folder())'